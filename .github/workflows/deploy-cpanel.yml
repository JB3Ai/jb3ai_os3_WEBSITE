name: Deploy to cPanel

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build main app
        run: npm run build

      - name: Verify FTP-safe dist paths
        shell: bash
        run: |
          set -euo pipefail
          invalid=0
          while IFS= read -r -d '' file; do
            if printf '%s' "$file" | LC_ALL=C grep -q '[^ -~]'; then
              echo "Non-ASCII deployment path: ${file#./}"
              invalid=1
            fi
          done < <(find dist -print0)
          if [ "$invalid" -ne 0 ]; then
            exit 1
          fi

      - name: Deploy main dist to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.CPANEL_FTP_SERVER }}
          username: ${{ secrets.CPANEL_FTP_USERNAME }}
          password: ${{ secrets.CPANEL_FTP_PASSWORD }}
          protocol: ftps
          port: 21
          security: loose
          local-dir: ./dist/
          server-dir: public_html/
          timeout: 600000
          log-level: verbose
          dangerous-clean-slate: false
          state-name: .ftp-deploy-sync-state-v2.json
          exclude: |
            **/.git*/**
            **/.git*
            **/node_modules/**
            **/.DS_Store
