<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Allow direct access to index.html
  RewriteRule ^index\.html$ - [L]

  # Canonical route aliases for static Wayne project exports
  RewriteRule ^isidore$ /isidore/ [R=301,L,NC]
  RewriteRule ^nms$ /nms/ [R=301,L,NC]
  RewriteRule ^wayne/isidore$ /isidore/ [R=301,L,NC]
  RewriteRule ^wayne/nms$ /nms/ [R=301,L,NC]
  RewriteRule ^wayne/isidore/(.*)$ /isidore/$1 [R=301,L,NC]
  RewriteRule ^wayne/nms/(.*)$ /nms/$1 [R=301,L,NC]

  # Fallback for deployments serving repository root (no built dist/ published)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{DOCUMENT_ROOT}/public/isidore -d
  RewriteRule ^isidore(?:/(.*))?$ /public/isidore/$1 [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{DOCUMENT_ROOT}/public/nms -d
  RewriteRule ^nms(?:/(.*))?$ /public/nms/$1 [L]

  # Force SPA handling for demo routes even if legacy demo/ folders exist in the web root
  RewriteRule ^demo(/.*)?$ /index.html [L]

  # Exclude static directories and files from SPA rewriting
  RewriteRule ^(brochures|public|clipboard|kids-goexplore|os3grid|dadchef|Wayne|wayne|isidore|nms)(/.*)?$ - [L]
  RewriteRule \.(pdf|png|jpg|jpeg|gif|css|js|svg|woff|woff2|ttf|eot)$ - [L]

  # Rewrite remaining requests to index.html for SPA routing
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
